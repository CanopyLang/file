# canopy/file — Features

- [ ] File reading progress events (onProgress callback)
- [ ] File.Download.saveAs — Save-as dialog with suggested filename
- [ ] MIME type detection from content (magic bytes)
- [ ] File size formatting utilities (`formatSize : Int -> String`)
- [ ] `readAsArrayBuffer : File -> Task x Bytes` — Alternative to toBytes
